# Repo Overlay -- lra-pdf-extractor

Repo identity: PDF/source ingestion and candidate extraction tooling.

PDF/source ingestion, bibliography normalization, candidate extraction, OCR/cleanup assistance, and staged review outputs. Candidate artifacts must be reviewed in the owning repo before integration.

Success gates:
- `python -m pytest tests`

No manifest-backed LLM capability is exposed for repo kind `pdf_extractor` yet.
