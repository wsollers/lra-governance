# Repo Overlay -- lra-reading-categorizer

Repo identity: Reading library categorization UI.

Human-in-the-loop UI and taxonomy tooling for categorizing the local mathematical PDF collection. Outputs are local reading-library placement and review artifacts, not direct volume/source-profile integration.

Success gates:
- `python -m unittest discover -s tests`

No manifest-backed LLM capability is exposed for repo kind `python_tool` yet.
