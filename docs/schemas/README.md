# LRA Schemas

This directory will hold canonical schema documentation and migrated schema
files for governance, extraction, auditing, and generated artifacts.

Current schema sources remain under `constitution/schema/` and
`constitution/schemas/` until a later migration phase.
